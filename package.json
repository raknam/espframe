{
  "name": "immich-frame",
  "private": true,
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "minify": "esbuild docs/public/webserver/app.js --minify --outfile=docs/public/webserver/app.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "vitepress": "^1.6.3"
  }
}
