interface:
  display_name: "Release"
  short_description: "Create GitHub releases and start firmware builds"
  default_prompt: "Use $release to create the next GitHub release and watch the build action."
