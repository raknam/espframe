# =============================================================================
# BUILD - Guition ESP32-P4 JC1060P470
# =============================================================================
# Thin CI wrapper: sets product name, enables AP mode for captive portal,
# and pulls the full config from the device's packages.yaml.
# =============================================================================

substitutions:
  name: "immich-frame-7inch"
  friendly_name: "Immich Frame 7inch"

esphome:
  name_add_mac_suffix: true

packages:
  espframe: !include ../guition-esp32-p4-jc1060p470/packages.yaml

wifi:
  ap:

captive_portal:
